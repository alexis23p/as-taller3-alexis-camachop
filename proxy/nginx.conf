events {}
http {
    server {
        listen 80;
        server_name _;

        location /api/ {
            proxy_pass http://api:8000/;
            proxy_set_header Host $host;
        }

        location / {
            proxy_pass http://webapp:5000/;
            proxy_set_header Host $host;
        }
    }
}
